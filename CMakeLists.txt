cmake_minimum_required(VERSION 3.16)

# Disable ESP-IDF component manager usage for this minimal milestone.
set(ENV{IDF_COMPONENT_MANAGER} "0")

include($ENV{IDF_PATH}/tools/cmake/project.cmake)
project(OpenAudioOS)
